# Flox
This language will basically be Lox (Crafting Interpreters), but with most everything
being an Expression.


```
var a = 0;
var b = 1;
var c = if (a)      { "NOPE" }
        else if (b) { "YEP"  }
        else        { "NOPE" };
```
